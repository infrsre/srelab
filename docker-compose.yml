services:
  docs:
    build:
      context: .
      args:
        DOCUSAURUS_BASE_URL: "/"
    ports:
      - "8080:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
